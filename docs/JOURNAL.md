# Журнал «Эволюции»

Рабочая память проекта: как устроено, почему так, что менялось и что отложено. Пишется для
того, кто продолжает без памяти о прошлых разговорах, — в первую очередь для Claude.
**После каждого заметного изменения — запись в «Хронику».**

## 1. Откуда игра и как работаем

- Владелец — один человек, игра для себя, общение по-русски, простыми словами.
- Репозиторий называется `Opushka`: сначала здесь была «Опушка» (спокойная игра про лес и
  деревню, перенесённая из приложения Core). Владелец решил делать другую игру — **эволюцию
  с нуля**, и выбрал «вместо Опушки». Опушка осталась в истории git (последний её коммит —
  «Огород и звери», 07efc92) и её можно вернуть.
- Задумка владельца: развитие с бактерии. Можно собирать своего персонажа (крылья, ласты…),
  но часть получаешь, только победив того, у кого она есть, и выпадает она не всегда.
  Выбор — хищник или травоядный. Потом, может быть, до космоса, но это не решено.
- **Сейчас делаем только первый этап — клетку — и прорабатываем его как следует.** Владелец
  прямо сказал: второй этап (выход на сушу) не делать, пока клетка не хороша.
- Договорённости: присылать ссылку на APK, когда сборка готова; большие перемены — сначала
  показать скриншотами; честно говорить, что не проверено на телефоне (эмулятора нет, звук
  я не слышу).
- Пакет на телефоне тот же, что у Опушки (`com.nonedeity.opushka`, тот же ключ) — новая
  игра встала на её место. Имя на телефоне — «Эволюция».

## Как устроена игра

- `scripts/core/` — правила, без экрана, покрыты тестами:
  - `content.gd` — справочники: 15 частей (`PARTS`), 15 видов (`SPECIES`), 10 размеров
    (`LEVELS`: порог накопленной ДНК и радиус), цвета, 11 задач.
  - `evolution.gd` — твой вид, это и есть сохранение (`user://evolution.json`): вся
    накопленная ДНК (от неё размер), тело (части с углами в градусах, 0 — нос, +90 —
    правый бок, 180 — хвост), открытые части и их уровни, копии к следующему уровню
    (`shards`), встреченные виды, счётчики, задачи. Редактор: `can_place/place/remove`.
    Свободная ДНК = накопленная + 10 − цена стоящих частей: поставил — потратил, убрал —
    вернулось. Рот один: новый встаёт вместо старого. Части не теснее 25°, углы по 15°.
  - `creature.gd` — одна клетка: тело из частей → `rebuild()` считает скорость, здоровье,
    укус (рот), шипы, панцири, яд, ток, свет, зрение. Всё растёт с размером (урон ~r^0.7,
    здоровье ~r^1.5). Уровень части: +20% силы за уровень; для еды — вдвое слабее.
  - `pond.gd` — океан: ты, клетки, еда (водоросли и мясо), выпавшие части (капсулы).
    Бесконечный, не сохраняется: всё появляется за краем экрана и исчезает вдали. Один шаг
    (`step`) — движение, столкновения (раздвигаются по массе), драка, ток, еда, находки,
    гибель, появление новых. Всё, что стоит показать, — в `events`.
- Драка — касанием. Часть действует, если смотрит на того, кого коснулась (в пределах своей
  дуги). Рот кусает раз в 0,55 с; шип колет раз в 0,5 с каждого; яд травит на 3 с;
  панцирь со своей стороны режет урон; «Рывок» (раз в 1,6 с) — таран ×1,5 и удар с разгона
  даже без оружия. Свои (один вид) друг друга не трогают. Победа засчитывается тебе, если
  ты ранил в последние 3 с — тогда ДНК за победу и выпадение частей с их шансами. Выпадает
  только то, что у вида есть (проверяется тестом).
- Поведение: мирные едят водоросли и убегают от опасных; пугливые — издалека; дрейфующие
  медленно плавают и колются; хищники гонятся за теми, кто не больше них в 1,3 раза, но
  выдыхаются через 5 с и 4 с отдыхают — оторваться можно; великан разворачивается шипами
  к обидчику, Левиафан охотится. Первые 25 с после старта хищники не появляются.
- Смерть: новая клетка появляется поодаль, всё найденное остаётся.
- Экран: `scripts/view/pond_view.gd` (вода с пылинками в два слоя, еда, капсулы, клетки,
  эффекты, камера: чем больше клетка, тем дальше), `backdrop.gd` (градиент и лучи — слой
  под миром), `scripts/cell_art.gd` (как рисуются клетки и каждая часть: у части своя
  система — x наружу от тела, единица = 1/20 радиуса), `scripts/ui/` (HUD, редактор с
  атласом, настройки, джойстик, «Рывок», стрелки к находкам).
- Управление: джойстик или «плыть за пальцем» (держишь палец — плывёт к нему).

## 2. Сборка и проверка

- `.github/workflows/build.yml`: Godot 4.3 → андроидные шаблоны (кэш) → импорт → тесты →
  путь к SDK в настройках редактора → номер сборки в `export_presets.cfg` → экспорт →
  релиз `build-N` с `evolution.apk`.
- Подпись: `android/release.keystore` (алиас `opushka`, пароль `opushka-release`) лежит в
  репозитории. Без постоянного ключа телефон не ставит новую сборку поверх старой. Для
  личной игры вне магазина достаточно; для магазина — вынести в секреты.
- Только `arm64-v8a` — меньше APK.
- Тесты: `godot --headless -s tests/run.gd`, файлы `tests/test_*.gd`, методы `test_*`.
- Скриншоты здесь: `xvfb-run -a godot --path . --rendering-driver opengl3 --resolution
  360x720 --write-movie out.png --quit-after N` — пишет кадры `out0000000N.png`.
- Иконки: `godot --headless -s tools/make_icons.gd` (SVG → PNG самим Godot).
- Звуки: `node tools/make-sounds.js` (синтез, детерминированный; новые — в конец файла).
- **Проверка звуков без ушей**: `node tools/sound-check.js [файлы] [--sheet=лист.png]` —
  таблица (пик, громкость по-ушному ~LUFS, громкость в игре с `LEVEL`, сколько останется в
  динамике телефона, доли частот, яркость) и список подозрительного: перегруз, щелчки,
  шов петли, «пропадёт в телефоне», «режет ухо», частый звук громче редких. Лист —
  волна и спектрограмма каждого звука (красная линия — 300 Гц, ниже телефон почти не
  играет; синяя — 2 кГц). Настоящий звук игры: при `--write-movie out.png` Godot рядом
  пишет `out.wav` (после всех шин, фильтров и эха) — его тоже можно проверить.
  В игре: Настройки → «Проверка звуков» — каждый звук по номеру, чтобы владелец мог
  сказать, какой не нравится (`--do="open:settings sounds:900"`).
- Баланс: `godot --headless -s tools/sim_bot.gd -- --minutes=60 --seed=2 --diet=plant|meat` —
  бот играет сам и пишет, на какой минуте вырос, погиб, что нашёл. За 60 игровых минут
  считает около минуты.
- Сценарии для скриншотов (аргументы после `--`): `--fresh --seed=42 --dna=300
  --unlock=spike:2 --body=jaws@0,spike@90 --color=3 --seen=zelenka,kusaka
  --body=eye@0@0 (третье — глубина) --spawn="*kusaka@150,-40;zelenka@-120,30" (звёздочка —
  сияющая) --do="pad:1,0:30 dash wait:20 open:editor|atlas|settings select:spike
  ghost:90,1 placeat:90,1 pick:0 mode:shape pull:0,1.4 shape:oval swipe:350,280,-200 close
  banner print"`. Окно для скриншотов — 720x360 (игра горизонтальная).


## 3. Грабли

- **Экспорт под Android молча падал** («Cannot export … due to configuration errors:» и
  пустой список). Причина — в исходнике 4.3 (`platform/android/export/export_plugin.cpp`,
  `has_valid_project_configuration`): без `rendering/textures/vram_compression/import_etc2_astc=true`
  экспорт невалиден, а сообщения нет. Включено в `project.godot`. Искал так: скачал
  шаблоны, собрал поддельный SDK из пустых `adb`/`apksigner` и повторил экспорт здесь.
- `var x := a and b` не компилируется («Cannot infer the type»), если в выражении есть
  значение из словаря или нетипизированного цикла. Писать тип явно: `var x: bool = ...`.
- `set_anchors_preset(PRESET_FULL_RECT)` меняет только якоря — размер остаётся нулевым.
  Растянуть на весь экран: `set_anchors_and_offsets_preset(...)`.
- Метод `_set(key, value)` в своём скрипте перекрывает встроенный `Object._set` — назвал `_apply`.
- `queue_free()` убирает узел только в конце кадра: при перестройке листа старое
  содержимое ещё держит ширину. Сначала `remove_child`, потом `queue_free`.
- Подпись с переносом строк без ширины (`custom_minimum_size.x`) растягивается в столбик.
- Внутренний класс нельзя назвать `Noise` — совпадает со встроенным классом Godot.
- В тестах числа сравниваются с допуском: шаги ходьбы по 0.2 клетки дают 31.4999…

- `for m in массив_из_filter()` — переменная без типа: `var x := m.поле` не компилируется.
  Писать `for m: Creature in ...`.
- `Gradient` уже содержит две точки (0 и 1): `set_color(0/1)` меняет их, `add_point` — новые.

## 4. Хроника

- **Эволюция, этап клетки** (заменила Опушку):
  - Правила: 15 частей (фильтр, челюсти, клыки, хоботок, реснички, жгутик, мощный жгутик, шип,
    большой шип, панцирь, толстая мембрана, ядовитая железа, электроклетка, глазок,
    хлоропласт), 15 видов от Крошки до Левиафана, 10 размеров, 11 задач. 115 проверок
    (`tests/test_evolution.gd`, `tests/test_pond.gd`).
  - Баланс по боту: травоядный доходит до 10-го размера примерно за 50 мин, хищник — за час;
    человеку, думаю, в 1,5–2 раза дольше. Хищник быстрее собирает части. Уровни частей: копии
    к следующему — 1, 2, 3, 4 (всего 10 после первой); на пятом копия даёт +10 ДНК.
    Сначала порогов было меньше, и бот проходил всё за 15 минут — подняты.
  - Редактор: палитра (открытые, потом закрытые с замком и «где взять»), клетка крупно —
    ведёшь пальцем по краю, видно, куда встанет часть (зелёная — можно, красная — нельзя),
    отпускаешь. Нажал на часть на клетке — «Убрать». Зеркально, 8 цветов, имя вида.
    Атлас: встреченные виды, поведение, размеры, победы, что роняют и с каким шансом.
  - Звуки синтезированы заново (`tools/make-sounds.js`): еда, укус, удар, боль, победа,
    находка, новая часть, рост, рывок, ток, яд, гибель, кнопки, редактор, фоновая глубина.
  - Иконка — клетка с глазом, жгутиком и челюстью.

- **Горизонталь, прокрутка, форма тела, туман, сияющие** (просьба владельца после первой
  сборки: «игра всегда горизонтально», «атлас и тело не листаются», «тело менять как
  угодно», «глаз не ставится в центр», «глаз сделан по приколу — сделай туман»):
  - Только горизонтально (`window/handheld/orientation=4`, датчик — любой бок). Кнопка
    поворота и настройка ориентации убраны. Редактор разложен в две колонки.
  - Прокрутка: встроенная прокрутка Godot срабатывает, только если касание дошло до самого
    `ScrollContainer`, а плитки-кнопки его перехватывали. Своя `scripts/ui/drag_scroll.gd`
    слушает касания в `_input`, после 14 точек сдвига листает и шлёт
    `NOTIFICATION_SCROLL_BEGIN` — кнопки под пальцем отменяют нажатие. Есть инерция.
    Проверка сценарием: шаг `swipe:x,y,dy` (координаты окна, а не холста).
  - Форма тела: `evo.shape` — 16 радиусов (0 — нос), 0,6–1,6; между точками — сплайн
    Катмулла — Рома (`Content.shape_at`). В редакторе режим «Форма и цвет»: палец тянет
    край (соседние точки мягче), «Зеркально», готовые формы (круг, овал, капля, широкий,
    звезда, боб, клякса), «Сгладить». В драке тело — круг: радиус = рост × средний радиус
    формы (0,8–1,25). У видов теперь тоже формы (`"shape"` в `SPECIES`).
  - Части внутрь тела: у глаза, хлоропласта и электроклетки `inner` — их можно ставить
    куда угодно, хоть в середину (`d` — глубина: 1 край, 0 середина, шаг 0,1). Теснота
    считается по расстоянию между точками частей (`PART_GAP` = 0,42 радиуса).
  - Туман (`scripts/view/fog.gd`, шейдер на слое над миром): видно `vision` =
    (95 + 1,6·рост)·(1 + 0,45·глаза). Без глаз — только рядом, 2–3 глаза — почти весь
    экран. Новые виды «встречаются» только в пределах видимости.
  - От себя: сияющие особи (3% при появлении, не великаны) — золотые, с искорками,
    пугливые, крепче в 1,5 раза, ДНК за победу ×3, часть выпадает всегда. Счётчик в атласе.
  - 133 проверки.

- **Камни, неотбиваемые части, великаны, пара, сложность и сохранения, анимации**
  (просьба владельца):
  - Камни (`Content.ROCKS`: камень, валун, кристальная глыба): неподвижны, растут с тобой,
    крошатся рывком, укусом и шипом (бур — втрое сильнее). Из них — части, которых нет ни у
    кого: бур, каменная кожа (броня со всех сторон), кристалл (светится, раздвигает туман).
    Шансы не 100%. Владелец написал «эта часть её покупается» — магазина нет, понял как
    «добывается только из камней»; уточнить при случае.
  - Части «не выбить» (`source: "mob"`): щупальце (хватает и замедляет), фонарик (виден в
    тумане — шейдер тумана получает до 8 «окошек»), пластины (броня со всех сторон), рог.
    Есть у Фонарщика, Щупальцевика, Броненосца, Великана, Пожирателя. Игроку их не поставить
    и из сохранения не протащить. В атласе — «не выбить».
  - Великаны (`"scale"` в виде): Гигантская амёба (мирная) и Пожиратель (хищник) — всегда
    в 4 и 3,2 раза больше тебя, по одному за раз.
  - Пара: тело меняется только после встречи. Кнопка ♥ зовёт пару поодаль (`pond.mate`),
    розовая стрелка/сердечко показывает, где она (и в тумане). Коснулся — «Поколение N» и
    редактор. Книга сверху — атлас (тело там только смотреть).
  - Сложность (`Content.DIFFICULTY`): урон по тебе, шанс выпадения, скорость хищников; на
    тяжёлой гибель отнимает четверть пути до следующего размера. Сохранения — три ячейки
    (`scripts/saves.gd`, `user://slot_N.json`), меню `scripts/ui/menu_panel.gd`; старый
    `evolution.json` переносится в первую. Проверочный запуск с аргументами — ячейка 0, не
    сохраняется (`--menu` — показать меню).
  - Анимации: тело вытягивается по ходу, дышит; рот раздувается при глотке; рост —
    вздрагивание; новые клетки проявляются из мути; погибшие съёживаются и тают; находка
    влетает в клетку; осколки камня; сердечки при встрече; замедленные — розовое кольцо.
  - Звуки: камень, зов пары. 166 проверок. Бот теперь растёт медленнее — он упирается в
    камни (плывёт напрямую), человек обходит.
  - Шаги сценария: mate, mated, rock:вид,dx,dy; `--difficulty=hard`.

- **Большое обновление** (владелец выбрал из списка пункты 1, 2, 5, 6, 7, 8, 10, 11, 12,
  13, 14, 17, 18, 19 и добавил три своих):
  - Воды (`Content.BIOMES`, `pond.biome_at` по двум шумам): мелководье, глубина (темнее,
    туман гуще, невидимки), горячие источники (жгут без термооболочки), холодное течение
    (медлишь без жировой капли). У каждой — свои жители (`"biomes"` у вида). Фон и надпись
    под здоровьем меняются; первая встреча — баннер.
  - Течения (`current_at`): узкие полосы вдоль линий шума, несут и клетки, и еду. В тестах
    `pond.nature = false` — без вод и течений.
  - Стаи (`"school"`): мальки и зубастики появляются кучкой, держатся вместе, при угрозе
    разлетаются. Паразит-пиявка цепляется и пьёт здоровье, рывок стряхивает.
    Невидимка-призрак: без глаз почти не виден, и стрелки на него нет.
  - Логова (сетка 2600, в глубине/горячей/холодной воде): хозяин сидит дома, на 60% зовёт
    подмогу, на 30% — ярость и удар волной. Полоса босса сверху, фиолетовые стрелки с
    черепом к логовам. Награды — части только из логов: ядро жизни, шипастая броня,
    реактивный мешок.
  - Свита: каждая встреча с парой — потомок (до трёх), плывёт следом и бьёт твою цель;
    после правки тела становится похож (`resync_allies`). Погиб — новый через 45 с.
  - Умения — вторая кнопка над «Рывком» (есть, если стоит часть): чернила, щит, разряд,
    всасывание. Мобы с такими частями тоже ими пользуются.
  - Узоры и второй цвет (Форма и цвет), вкладки «Родословная» (`evo.history`, снимок при
    закрытии редактора после пары) и «Достижения» (18 штук + коллекция).
  - Арена — кнопка у заполненной ячейки: волны врагов в круге, рекорд `arena_best`; там не
    растёшь и части не выпадают. Песочница — кнопка в меню, своя ячейка 4: всё открыто,
    ДНК без счёта, ♥ сразу открывает тело, в атласе «Призвать».
  - Музыка (`music_calm`/`music_fight`, синтез в `make-sounds.js`): спокойная, а когда
    гонятся, ранили или бой с хозяином — боевая, переход плавный. Выключается в настройках.
  - Просьбы владельца: океан больше не пустеет (новые появляются ближе, уплывшие исчезают
    дальше, а если рядом долго никого — кто-нибудь приплывает сам); больше злых (острозуб,
    зубастики, жалильщик и др., у агрессивных вес ×`aggro` сложности); сверхсложная
    сложность — одна жизнь (съели — ячейка удаляется, «Вид вымер»), хищников больше,
    туман гуще.
  - Баланс после этого: существ рядом больше, и хищник-бот рос до 10 за 16 мин. Мясо дешевле
    (`MEAT_DNA` 3 → 0,8), ДНК за победу вдвое меньше, место съеденного пустует 20 с
    (`KILL_QUIET`). Сейчас: травоядный ~50 мин, хищник ~40 мин до 10-го размера.
  - Шаги сценария для снимков: `goto:вода`, `lair:вид`, `hp:доля`, `ability`, `arena`,
    `sandbox`, `gen`, `part:id,угол,глубина`, `pattern:узор,цвет2,цвет`, `tab:tree|awards`.
  - 245 проверок.

- **Правки после большого обновления** (владелец: «боссы очень сложные», «существа по 1–3,
  не больше», «существ многовато», «глаз почти не получить», «проработай всё»):
  - Хозяева логов: в 1,8–2 раза больше тебя (было 2,4–2,8), бьют на 35% от своего размера
    (`LAIR_DAMAGE`), устают после погони (`LAIR_REST` 3 с — окно, чтобы бить), сами не
    лечатся — только дома, медленно и если тебя нет 8 с. Подмога — двое, ярость мягче, удар
    волной слабее и раз в 5,5 с, за секунду до него — красный круг и подсказка. Шипастая
    броня стража возвращает вдвое меньше. Логова — с размера 4–5. Проверка: бот «в лоб» с
    хорошим телом на 6-м размере побеждает любого за 7–22 с (раньше — никого за 5 мин);
    тест `хозяин_логова_проходим`.
  - Стайки по 2–3 (было 5–7). Существ вокруг меньше: `_mob_target` = 6 + размер/3, не
    больше 9 (было до 30); на экране обычно 2–5. Если рядом стало пусто — через 2,5 с
    кто-то появляется впереди по ходу (раньше таймер залипал и ждал до 20 с).
  - Глаз: Глазун теперь мирный и медленный, с 1-го размера, чаще (вес 3,5) и роняет глаз
    в 50%; глаз ещё у Фонарщика, Чернильника, Удильщика. Бот находит глаз за 1–5 мин.
  - Найдено при проверке: паразиты ехали за тобой после гибели; хозяин логова, уплывший
    из памяти, больше не появлялся; после гибели можно было возникнуть у чужого логова;
    на арене край прятался в тумане и в первой волне мог выйти великан; стрелок к логовам
    бывало несколько. Всё поправлено.
  - Баланс с меньшим числом существ: мясо 1,3 ДНК, победа 0,8 + 1,1·ранг. Бот: 5-й размер
    на 16–26 мин, к часу — 8–10-й. Бот теперь ставит термооболочку и жировую каплю.
  - 248 проверок.

- **Туман, круги водорослей, дрейф, рывок-часть, мясо по размеру** (владелец: «даже с
  глазом туман», «сделай круг, по краям кусочки еды, сам круг нельзя есть, не на месте»,
  «камень тоже не фиксированный», «отдельные кусочки не убирай, сделай их меньше»,
  «рывок — если поставить штуку для рывка», «мясо по размеру убитого»):
  - Обзор с глазами считает `Pond.vision()` от экрана: без глаз — только рядом, один
    глаз — почти весь экран, два — весь (глубина и сложность по-прежнему сужают).
    `Creature.vision` — теперь только «без глаз».
  - Круги водорослей (`pond.colonies`, `add_colony`, `_floating`): несъедобная подстилка,
    по краю 10 мест под еду (`food.col/slot`), съеденное отрастает раз в 3,5 с. Круг плывёт
    своим ходом и по течению, над ним можно проплыть (сплошным он мешал — боты застревали).
    Вокруг 4 + размер/4 круга. Одиночных водорослей 45 (было 120). Грабли: одиночные
    считались вместе с кусочками на кругах и почти не досеивались — теперь отдельно.
  - Камни тоже дрейфуют (`vel/drift`), упрёшься — чуть подаются (`_nudge`).
  - Рывок — только с «Толчковым пузырём» (`sac`, 8 ДНК, `dash: true`; реактивный мешок
    тоже даёт рывок). Открыт с начала, но не стоит: задача №3 — поставить его через пару.
    Кнопка «Рывок» появляется, только когда он есть. Старые сохранения: пузырь ставится
    сам, если есть место и ДНК. Паразит без рывка отпадает сам через 12 с.
  - Мясо: общее — как раньше (радиус/8), но кусков 1–5 и они размером с погибшего
    (`r = радиус·0,55/√кусков`), ДНК и лечение — по `value` куска.
  - Острозуб — со 2-го размера: на 1-м новичка без рывка съедали раз за разом.
  - Бот: копит на пузырь, не гоняется, пока нечем бить. Сейчас 2-й размер на 1–11 мин,
    10-й на 35–54 мин.
  - 263 проверки.

- **Магазин, форма тела со смыслом, анимации** (владелец: «магазин, улучшения типа защиты
  от жары — не лепить на клетку»; «не хватает места, растягивать бесполезно — только
  визуально»; «анимации, например на получение новой части»):
  - Магазин (`Content.UPGRADES`, `evo.upgrades/buy/upgrade_cost/shop_spent`,
    `scripts/ui/shop_panel.gd`, кнопка-кошелёк сверху справа): термозащита, жировой слой,
    лишнее место (до +3), крепкая оболочка, заживление, большой желудок (+10% ДНК),
    маскировка, сильный рывок. ДНК за покупку уходит насовсем. Действует через
    `Creature.bonus` (`_apply_bonus`). Пока магазин открыт — пауза.
  - Термооболочка, жировая капля и хроматофоры больше не части (`source: mob` — остались
    у существ для вида). Кто их добыл раньше — получает улучшение даром (`evo.gifts`, ДНК
    не списывается). Из существ вместо них выпадает другое.
  - Форма: `Content.shape_stats` — площадь, обвод, вытянутость. Обвод даёт места (+1 за
    каждые 10% сверх круга, до +4), площадь — здоровье (√площади), вытянутость вперёд —
    скорость, большая площадь — чуть медленнее. Зазор между частями считается по настоящему
    контуру (`evo.anchor` — не статическая, учитывает форму): на большом теле просторнее.
    Мест за размер на одно больше (3 + размер). Сжать тело так, что стоящие части не
    поместятся, нельзя — редактор объясняет. В «Форма и цвет» — что даёт форма сейчас.
  - Сохранение: форма и покупки читаются до тела; лишние части снимаются с конца, а не
    сбрасывают тело к стартовому.
  - Анимации: находка — значок части крупно с лучами и искрами, потом летит к ♥ (у
    повышения уровня — зелёный с «+1»); ♥ с «!» дышит и светится; рост — плашка размера
    подпрыгивает, экран теплеет; поставленная часть в редакторе вырастает с перелётом и
    золотым кольцом; покупка — карточка вспыхивает, значок прыгает с искрами, ДНК
    подпрыгивает; редактор, настройки и магазин открываются мягко (`Kit.pop_in/bounce`).
  - Шаги сценария: `open:shop`, `buy:id`, `reveal:id`, `kill`.
  - Бот: покупает жару/холод. 10-й размер у бота теперь на 32–42 мин (мест больше).
  - 279 проверок.

- **Лаги** (владелец: «игра лагает»). Замер (`--profile`: время частей кадра, вызовы
  рисования, треугольники): логика мира — 0,4 мс, тормозило рисование.
  - Фон пересобирал текстуру градиента и заново отправлял её в видеокарту каждый кадр
    (появилось с водами) — теперь градиент цветами вершин, без текстуры.
  - Пылинки воды рисовались сотнями отдельных кружков (~300 вызовов из 500) — теперь
    один `draw_multiline` на слой. Течения — один `draw_multiline_colors`.
  - Водоросли и мясо — готовыми картинками (`CellArt.plant_texture/meat_texture`), одна
    текстура на всех — видеокарта рисует разом.
  - Туман не считается, когда обзор шире экрана; спрятанные подсказки и баннер не
    рисуются; стрелки перерисовываются, только когда есть.
  - Итого: вызовов рисования 495 → ~200, треугольников 29 тыс. → ~10 тыс.
  - Грабли: в `_process` не менять градиенты/текстуры каждый кадр; много мелочи рисовать
    одной командой; невидимое (alpha 0) — прятать `visible = false`.

- **Баланс по размеру, твёрдые круги, гиганты, новые враги и части, мини-карта, голоса**
  (владелец: «на 8 размере все слабые», «существ больше», «через круги нельзя
  проплывать» + выбрал пункты 4, 5, 6, 9, 12):
  - Размер существ (`pond.spawn`, `grow_k(def)`): вид такой, как задуман, на размере, с
    которого появляется; перерастаешь его — он подрастает как (твой радиус / радиус его
    первого размера)^0,6, части сильнее на +1 уровень за каждые 3 размера (до +2). Первая
    попытка (все растут как ^0,8) раздула Левиафана до 3,4 тебя — боты гибли по 30–68 раз
    за час; сейчас 2–24, 10-й размер к 40–50 мин.
  - Существ вокруг 8 + размер/2 (до 14). Круги снова твёрдые (`_colony_contacts`), а
    существа огибают круги и камни (`pond.avoid` в `_move`).
  - Бродячие гиганты (`behavior: roamer`, `_roamers`, раз в 2,5–4 мин с 5-го размера):
    Старый кит (мирный, отбивается; китовый ус — лучший рот для всеядных) и Морской змей
    (охотится, с 6-го; змеиный хвост — скорость и рывок). Плывут мимо и уходят.
  - Враги: Плевун и Иглострел (`shooter`: держат дистанцию и стреляют, `shots`, камни и
    круги заслоняют), Делитель (`splits`: ранен до половины — двое поменьше), Обманка
    (`ambush`: выглядит водорослью, пока не подплывёшь; с двумя глазами видно), Клешнец,
    Прилипало.
  - Части: плевательная железа и игломёт (`gun`: стреляют сами во врага впереди), клешня,
    светлячок (`light`: туман шире), прилипала (`remora`: касаешься крупного этой стороной
    — едешь на нём, ешь крохи, он не кусает; рывок/ход прочь — отцепиться).
  - Мини-карта (`hud.MiniMap`, настройка «Мини-карта»), перерисовка 8 раз в секунду.
  - Голоса: щебет, рык, гул, шипение, песня кита, плевок, деление (`make-sounds.js`),
    событие `voice` — когда существо впервые показалось и когда хищник погнался за тобой.
  - Мелкие на экране существа рисуются попроще (`simple`).
  - 319 проверок.

- **Тени в глубине, значки частей, разнообразие особей, драки видов** (владелец):
  - Тени (`backdrop.gd`, `Shadows`): 3 силуэта видов следующих 1–2 размеров (на 10-м —
    самые большие), крупные, медленно плывут и почти не сдвигаются, когда плывёшь
    (глубина 0,3) — видно, что это фон. Отдельный слой с тёмно-синим `modulate`, чтобы и
    части были тенью. При росте: «их тени уже видны в глубине».
  - Значки частей (`CellArt.part_icon`): кружок цвета вида части (`KIND_COLOR`: рот,
    оружие, защита, движение, чувства, особое, умение), часть крупно: внешняя — из
    краешка тела, внутренняя — посреди клетки, краевая — дугой.
  - Разнообразие (`pond._vary`): оттенок, насыщенность, яркость, форма ±7%, у ~40% —
    пятна/полоски/кольца (у существ теперь `pattern`, `color2`).
  - Драки: хищники чужих видов — соперники (нападают и на чуть более крупных, до 1,6×);
    вооружённый, кого укусил чужой, даёт сдачи. Свои (один вид) не трогают друг друга
    никогда — тест `свои_не_дерутся`.
  - 323 проверки. Бот: 10-й размер 33–45 мин, гибелей 7–24 за час.

- **Гиганты вместо логов, тени быстрее, появление шире** (владелец: тени «стоят на месте»;
  «на каждом уровне новые гиганты, на фоне — только новые»; «убери боссов с логовом —
  пусть плавают где угодно»; «гиганты одни, без стаи»; «не показывай гигантов на карте»;
  «радиус появления узкий — на скорости пусто»):
  - Логов больше нет (`lair_at/lairs_near/_lair_think/_bosses` удалены). Все крупные —
    `behavior: roamer`, со `scale` от твоего размера: на каждом размере новый гигант (1
    амёба, 2 медуза, 3 пожиратель, 4 королева, 5 кит и страж, 6 великан, вихрь и змей,
    7 титан, 8 левиафан, 9 кальмар, 10 Древний). Вокруг не больше одного, раз в 1–2 мин
    (`_roamers`, `pick_giant` — новые для размера втрое чаще), без подмоги. Бьют с
    `GIANT_DAMAGE` 0,35, сами не лечатся. Полоса здоровья — пока гигант бьётся с тобой.
    Победа — событие `giant_beaten`; прогресс по-прежнему в `evo.lairs_beaten` (имя
    оставлено ради сохранений), задача и достижение переименованы.
  - На мини-карте гигантов нет. Тени — только гиганты следующих 1–2 размеров (на 10-м —
    Древний), по две, плывут 70–120 точек/с, размер от твоего (`player_r · scale`).
  - Появление: радиус шире (до экрана + 600), при быстром движении 70% — впереди по ходу
    (`_spawn_angle` для существ, еды, кругов, камней), досев до 5 за раз, исчезают дальше.
  - Баланс после этого: победа 0,6 + 0,9·ранг, мясо 1,1, еда на круге отрастает за 4,5 с.
    Бот: 10-й размер 32–47 мин, гибелей 5–19 за час.
  - Грабли: на один коммит GitHub однажды запустил сборку дважды — номера сдвинулись, и я
    дал ссылки на прошлые версии. Теперь номер сборки сверяю по описанию релиза («Коммит …»).
  - 338 проверок.

- **Гиганты мельчают и становятся стаями, мелочь пропадает, вод больше нет** (владелец:
  «я расту — остальные для меня меньше, гиганты тоже слабее; когда уже не гиганты — пусть
  стаей»; «первые маленькие станут слишком маленькими — пусть пропадают»; «убери биомы,
  пусть вода меняется только с уровнем, просто на вид»):
  - Гигант — на своём размере и ещё двух (`GIANT_LEVELS` = 2, `is_giant_now`). Размер
    (`giant_size`): `scale · 0,75^шаг` от тебя, не меньше 1,8×. Потом «бывший гигант»:
    с тебя ростом и дальше мельчает до 0,6× (минус 0,12 за уровень), в общем наборе
    (вес 0,3), стайкой 2–3. Мирный (`behavior()` → drifter, `_hunts`/`_wants_bite`),
    но если ты его ранил — 6 с даёт сдачи; бьёт ×0,6 (`FORMER_GIANT_DAMAGE`), награда
    за него — как за вдвое меньший ранг. У существа флаг `giant` (ставит `spawn`), по нему
    `is_giant`, полоса здоровья, мини-карта и `giant_beaten`.
  - С настоящего гиганта его главная часть (`drops[0]`) выпадает всегда.
  - Мелочь: вид, который для тебя меньше 0,3 твоего размера, больше не появляется
    (паразиты — исключение).
  - Биомов нет: убраны `BIOMES`, `biome_at`, эффекты вод (жара/холод/темнота), значок воды
    в HUD, поле `biomes` у видов, достижение «Воды», улучшения «жара/холод» в магазине.
    Цвет воды теперь только от размера — `Content.WATER` (10 пар цветов, темнее с ростом),
    свет тускнеет от 1,0 до 0,45. `evo.biomes_seen` читается ради старых сохранений.
  - Бот (6 прогонов по часу): сперва бывшие гиганты-охотники стаями убивали на 10-м
    размере чаще всех (до 27 гибелей за час, двое не доросли до 10) — поэтому стали
    мирными. Без вод пропал и «богатый» множитель водорослей мелководья: вернул общим
    ×1,15, мясо 1,1 → 1,25. Итог: травоядные — 10-й размер за 42–48 мин, гибелей 14–18
    за час; хищники растут медленнее прошлой версии (до 10-го за час дошли 2 из 6, за
    34 мин), гибелей 3–8. Бот теперь пишет, кто был рядом, когда он погиб.
  - 340 проверок.

- **Интерфейс удобнее и красивее** (владелец: «поработай над интерфейсом»):
  - В игре: размер, ДНК и здоровье — одна карточка (`hud.StatusCard`): кружок размера с
    кольцом роста, ДНК вспыхивает при прибавке (`hud.dna_gain()` вместо прыжка плашки),
    здоровье с числами, при малом — сердце бьётся. Имена `size_pill/dna_pill/hp_bar`
    остались и указывают на неё. Задача — шириной по тексту, с подсказкой в одну строку и
    полоской хода (`evo.goal_progress`). У круглых кнопок подписи (`RoundButton.caption`):
    Магазин, Атлас, Настройки, Пара. Сообщения — внизу посередине (не на клетке), полоса
    гиганта — тоже внизу, над ними (не налезает на крупные надписи).
  - Кнопки проседают под пальцем (`Kit.press_fx`, у `RoundButton` — своё). Вкладки и
    режимы — единым переключателем (`Kit.segmented`), сведения — тихими плашками
    (`Kit.info_chip`), не похожими на кнопки.
  - Редактор: имя вида крупно, под ним поколение и размер; ДНК и места — над клеткой;
    выбранная часть — карточкой со значком, описанием и кнопкой «Убрать»; на плитках —
    метка «уже на теле» (✓ или ×2); длинные имена ужимаются, а не обрезаются.
  - Атлас: фильтр (все, мирные, опасные, гиганты, камни), «Встречено N из M», невстреченные —
    тёмные силуэты «???» с размером, с которого водятся. Достижения: сверху полученные, потом
    те, где больше сделано.
  - Меню: в пустой ячейке сперва выбираешь сложность (описание под ней), потом «Начать»
    (для сверхсложной кнопка красная) — случайно не начнёшь. «Удалить» — значок корзины в
    углу, со вторым шагом «Удалить насовсем? / Нет».
  - Проверено снимками на 1600×720 и 1280×720. Игра только лёжа (`SENSOR_LANDSCAPE`), так
    что стоя не проверял.

- **Атмосфера** (владелец: «что придумать, чтобы было атмосферно» → «делай»; взяты 2, 3, 4,
  6, 7, 8, 11, 12, 13 из списка; 1, 5, 10 уже были — музыка боя, вибрация, виньетка):
  - События в океане (`Content.EVENTS`, `pond._world_events`, `start_event`): цветение
    (водорослей ×3, вода зеленеет, взвесь светится), буря (всех сносит `STORM_FORCE` 55 в
    одну сторону, взвесь несётся чёрточками), мёртвая зона (видно ×0,7, живых и водорослей
    вдвое меньше, сияющих ×4, темно), нерест (стайки малышей 0,35–0,5 тебя, каждые 15 с).
    Первое — через 200–320 с, потом раз в 280–420 с после конца; держатся 45–70 с, `event_k`
    нарастает и спадает за 4 с (`event_look` — что показывать, пока спадает). У событий свой
    генератор случайностей (`_event_rng`) — иначе сдвигались бы все проверки. На арене нет.
    HUD: объявление и плашка с остатком времени под карточкой состояния.
  - Блики солнца (`backdrop`, шейдер `CAUSTICS`): бесшовная сетка Вороного `art/caustics.png`
    (рисует `tools/make_caustics.gd`), два слоя скользят навстречу, светится их совпадение.
    Две выборки текстуры на точку — дёшево. К 7-му размеру гаснут, в тени гиганта и
    мёртвой зоне тоже.
  - Взвесь живая (`pond_view._stir`): ближний слой расступается перед клеткой и
    закручивается по бокам; рывок и гибель крупного пускают волну (`wave`), она отбрасывает
    пылинки кольцом. Со дна поднимаются цепочки пузырьков.
  - Звук под водой (`SoundBank`): шина «Вода» (фильтр верхов: с 16 кГц на 1-м до 3,2 кГц на
    10-м размере — глубже глуше) и «Даль» (ещё глуше и с отзвуком). `play(id, pitch, far)`:
    дальние — тише и через «Даль» (`main._far`). Меню-звуки — мимо воды (`DRY`).
  - Новые звуки (дописаны в конец `make-sounds.js`, старые файлы не изменились): heart,
    bubbles, creak, deep, swell. Раз в 7–16 с — далёкий звук океана; тень гиганта в глубине
    иногда «зовёт» (`backdrop.ghost_appeared`, не чаще раза в 25 с).
  - Сердце: здоровья меньше 30% — стучит, края экрана краснеют в такт (`_low_hp`).
  - Гигант рядом — свет гаснет (`backdrop.shade`, `_dim`). Удар с весом (`main._heavy`):
    удар гиганта или больше четверти здоровья, победа над крупным, гигант побеждён, гибель —
    мир замирает на 0,06–0,18 с, камера вздрагивает (`view.shake`). В событии `hit` теперь
    есть `giant` и `share`.
  - Бот: все шесть — 10-й размер за 40–44 мин, гибелей 2–15 за час (события добавляют еды).
  - Проверка: `--do="event:bloom"`, `myhp:0.2`. 352 проверки.

- **Рост: линька, отъезд камеры, волна; звук проверен приборами** (владелец: «1, 3, 5» из
  идей про рост; «ты же не можешь проверять звук — можно ли что-то сделать?»):
  - Линька (`pond_view._molt`): миг тишины (`_heavy` 0,3 с), свечение, старая оболочка
    трескается на 7 кусков по форме тела, они расходятся, поворачиваются и тают там, где
    ты вырос; плюс осколки. В событии `levelup` теперь `old_r` и `pos`.
  - Камера (`pond_view._grow_t`): 0,35 с подаётся к клетке (×1,1), потом рывком отъезжает
    чуть дальше нужного (×0,86) и за 2 с мягко возвращается.
  - Волна роста (`pond._growth_wave`): 2,5 с неуязвимости, паразиты отваливаются, всех
    рядом (кроме гигантов) отбрасывает и 3 с они плывут прочь (`Creature.scared_t`).
  - Звук — `tools/sound-check.js` (см. «Сборка и проверка»). Нашёл: гул гиганта, рык,
    зов из глубины, сердце, гул события почти целиком ниже 300 Гц — в телефоне их почти
    не слышно (−16…−31 дБ). Лечение — `presence()` в `make-sounds.js`: обертоны выше 250 Гц
    (гнём волну x·|x| и x³), ухо по ним слышит низкий тон; стало −9…−11 дБ. Случайных чисел
    там нет — прочие звуки не изменились. Запись игры показала общую громкость около
    −31 LUFS (тихо): на общий выход поставлен `AudioEffectHardLimiter` +7 дБ, потолок −1 дБ —
    стало −24, без перегруза. `place` −3 дБ, `heart` −1 дБ.
  - 356 проверок.

- **Редактор клетки: перетаскивание, перенос, отмена, цифры, фильтры, миниатюры,
  «Крупно», новое растягивание; пауза** (владелец: «1,2,4,6,7,10,13,14,17», «свернуть
  части — видна только клетка», «как растягивать, чтобы было понятно»):
  - Перетаскивание из списка (`editor._input`, `CarryLayer`): палец на плитке и повёл вбок
    (|dx| > 22 и в 1,3 раза больше |dy|) — тащишь часть, над клеткой видна примерка,
    отпустил — встала. `DragScroll` теперь листает, только если палец идёт в основном
    вверх-вниз (иначе перехватывал), и умеет `cancel()`.
  - Перенос: зажал часть на клетке и повёл — `evo.move(i, угол, глубина)`, ДНК не тратится;
    не встала — осталась на месте.
  - Отмена/возврат (↶ ↷): снимки тела, формы, цветов, узора (`_remember`, `_undo_step`,
    до 40 шагов, на время открытого редактора). Форма — один шаг за одно движение пальца.
  - Цифры «было → станет» (`_numbers`, `_what_if`, `_refresh_stats`): для выбранной части
    (на месте примерки или сбоку, с учётом «зеркально» и замены рта) и для взятой (без неё).
  - Список частей: вкладки по виду (Рот, Оружие, Защита, Движение, Чувства, Особое —
    с умениями), «Только найденные», «Снять всё» (`evo.clear_body`, со вторым нажатием).
  - Формы и узоры — миниатюрами (`Thumb`), цветов 16 (новые — в конец `COLORS`).
  - Тело видно всегда: из «Атласа» вкладка «Тело» открыта для просмотра, менять — после пары.
  - «Крупно» (`big`): панель справа прячется, клетка на всю высоту; для формы — узкая
    колонка инструментов. Глаз — показать/скрыть части (скрытые — бледные точки); в «Форме»
    части скрыты сами.
  - Растягивание: на краю 16 ручек; касание берёт ближайшую, и она ходит от того места, где
    взял (проекция хода на её направление — вбок палец может гулять). Кисть: узкая 22°,
    средняя 40°, широкая 70° — точки, что тянутся вместе, подсвечены по силе. Пока тянешь —
    прежний край пунктиром и «Места · Здоровье · Скорость» сверху. Кнопки «Больше, Меньше,
    Длиннее, Шире, Сгладить» (`evo.transform_shape`).
  - Пересборка редактора из обработки касания — отложенная (`_rebuild_soon`): иначе касание
    приходило в уже удалённый узел («!is_inside_tree»).
  - Пауза: кнопка в игре и сама при сворачивании (`pause_panel.gd`): Продолжить, Настройки,
    Выйти в меню.
  - Проверено настоящими касаниями: `--do="drag:x0,y0,x1,y1 body undo"` (`body` печатает
    тело, ДНК и форму). 366 проверок.

- **Гиганты чаще, колоссы, вода разных оттенков** (владелец: «гигантов почти не видно»,
  «супергиганты, которых не убить — чтобы удивляться; растёшь — они меньше», «не темней
  с уровнем, а разные оттенки»):
  - Гиганты: первый через 15 с, потом раз в 25–45 с. Главная причина редкости была в
    памяти: уплывший гигант держался в `mobs` до «экран + 3900» и не пускал нового.
    Теперь новому мешает только гигант ближе «экран + 800», а из памяти гигант уходит на
    «экран + 1500». Чтобы не стало смертельно: охотятся, только если ты ближе 0,75 их
    зрения; выдохлись в погоне — 25 с не охотятся (`Creature.bored_t`, `GIANT_BORED`);
    `GIANT_DAMAGE` 0,35 → 0,28. Бот: гибелей 0–18 за час, гиганты среди причин почти пропали.
  - Колоссы (`behavior: colossus`): Бездонный кит (300), Праматерь медуз (250), Спящий
    остров (380) — размер в точках мира, не от тебя (растёшь — они «меньше»). Раз в
    2,5–4 мин (первый через 90 с) проплывают наискось мимо тебя со скоростью 42
    (`_colossi`, `spawn_colossus`), неуязвимы (`invuln`), не кусают, всех расталкивают
    массой, на мини-карте их нет. «Ого!» — когда вплывает в кадр (`announced`). Камера
    отъезжает, пока он рядом (маленькому — почти вдвое), чтобы было видно, какой он.
    В счёт существ не входят (`pond.colossus`). В атласе — «колосс — не ранить».
  - Вода: `Content.WATER` — 10 оттенков одной яркости (бирюза, синий, зелёный,
    сине-фиолетовый…), свет больше не тускнеет с размером, блики на любом размере.
  - 381 проверка.

- **Тени уходят как надо, гиганты на карте, «Тяжёлая» — хищники хитрее** (владелец:
  «плыву вверх, тень вниз — а она не пропадает, тащится с экраном»; «гигантов и колосса
  на карте — красной точкой, как обычную клетку»; «играю на сложном — меня редко убивают»):
  - Тени (`backdrop`): сдвигались на 0,3 точки экрана за точку пути, а мир — на ~2 (зум) —
    почти «прилипали» к экрану. Теперь копится сдвиг мира в точках экрана (`_scroll`, с
    учётом зума — смена зума не дёргает), тени уходят на 0,7 от него (`DEPTH`). Блики —
    от того же `_scroll`.
  - Мини-карта: гиганты и колоссы — красные точки, как опасные клетки.
  - Сложность — повадки, а не только урон (`Content.DIFFICULTY`: sight, chase, grow, regen,
    pack; `pond._hard` — на 1–2 размере почти как на обычной, к 6-му в полную силу).
    Тяжёлая: замечают с 1,25 расстояния, гонятся ×1,5 дольше (гигант после погони скучает
    меньше), хищники растут с тобой со степенью 0,72 вместо 0,6 (остаются опасными на
    больших размерах), раны заживают ×0,65, 30% хищников — парами. Сверхсложная — ещё
    сильнее. Обычная не изменилась. Бот на тяжёлой (`--difficulty=hard`): было 16–29
    гибелей за час, стало 18–37 (на размерах 1–6 +40%, на 7–10 +32%).
  - 385 проверок.

- **Баланс частей: ток, стрелки, стая** (владелец: «электро убивает моментально — и меня,
  и если надену я»; «налепил 8 игл/плевков — всех ваншотаю»; «учитывай, что есть стая»):
  - Одинаковое не складывается в разы (`Creature.STACK` 0,3): первая стрелковая часть —
    полная сила, каждая следующая — треть; электроклетки так же (следующая — +1 цель, до 3,
    и чуть урона). 8 игломётов — ≈2,5× одного вместо 8×.
  - Ток: база 5 → 4, раз в 2,8 с вместо 2,2, за разряд не больше 30% здоровья цели
    (`Pond.ZAP_CAP`) — мгновенно не убивает. Разряд по тебе теперь «виден» (вспышка).
  - Дальность: игломёт 380 → 300, плевательная железа 320 → 270 — не бьют из-за края.
  - Стая: оружие потомков — вдвое слабее (`Creature.ALLY_POWER`), они же вдвое меньше.
  - Подсказки частей говорят, что вторая такая же слабее. 389 проверок.

## Отложено

- Второй этап (суша) — только когда владелец скажет, что клетка готова.
- Идеи для клетки: пещеры, «сон» (пауза с восстановлением), анимация деления при росте.
- Не проверено на телефоне: громкость музыки относительно эффектов, читаемость полосы
  босса и новой кнопки умения на маленьком экране.

## История: Опушка

Прежняя игра этого репозитория. Кратко, чтобы не потерялось:

- **Основа проекта**: Godot 4.3, сборка APK на GitHub, иконка, ключ, тесты.
- **Правила перенесены** (`scripts/core/`): `content.gd` (справочники), `world_gen.gd`
  (карта 64×64 из зерна, свой генератор случайных чисел — одинаковый везде),
  `village.gd` (состояние и действия). Отличие от версии в Core: ходьба плавная — позиция
  дробная, персонаж — круг радиусом 0.3 клетки, скользит вдоль препятствий; клетка
  действия — соседняя по взгляду или наискосок в пределах шага. 80 проверок.
- **Мир и интерфейс на экране** (`scripts/world/`, `scripts/ui/`, `scripts/main.gd`):
  земля кусками 16×16, деревья и постройки с сортировкой по глубине, ночь (затемнение +
  свет от костра и дома), частицы при рубке/сборе, камера с плавным следованием (×1.2).
  Джойстик и кнопка действия ловят касания сами по номеру пальца — можно идти и рубить
  одновременно. Плашки дня и сытости, задача, подсказки, полоска вещей, сумка (вещи и
  ремесло), настройки (управление, звук, экран, новый мир), звуки из Core, поворот экрана,
  автосохранение. Проверка сценарием: `-- --fresh --seed=42 --bag=axe:1 --do="goto:tree
  wait:80 act print"` (шаги: pad, wait, act, tap, craft, place, face, goto, open, dump, print).
- **Огород и звери**:
  - Огород. На траве растут дикая морковь («Выкопать» → морковка и семена) и
    подсолнухи (→ семечки). Их расставляет `world_gen.gd` по своему хэшу клетки, не трогая
    общий генератор, поэтому прежний лес остался на месте. Грядка стоит 4 ветки, по ней можно
    ходить. Лейка стоит 2 бревна и 2 ветки, воду набирают у пруда (5 поливов). Растёт только
    влажная земля: полив держится 720 минут, рост считается лениво, по времени
    (`beds`: g/last/wet). Поэтому политое вечером вырастает за ночь. Морковь растёт 360 минут,
    подсолнух 600.
  - Курятник: 6 брёвен и 6 веток у верстака, в нём две курицы. Насыпал 2 семечка — наутро
    2 яйца (`coops`: fed/eggs).
  - Звери (`scripts/core/animals.gd`) живут в настоящем времени, игровые часы от них не идут.
    Зайцы пугаются, если бежишь к ним. Если стоять тихо с морковкой (1,2 с), заяц подходит
    сам. Покормил — он ручной и ходит следом (в сохранении только число `pets`). Куры бродят
    у курятника, птицы вспархивают. Бабочки и светлячки (`critters.gd`) живут на своём
    CanvasLayer с `follow_viewport`, поэтому ночное затемнение их не гасит.
  - Кнопка действия: зверь забирает её, только если его можно покормить или перед тобой больше
    ничего нет. Иначе ручной заяц мешал бы поливать.
  - Новые звуки: dig, plant, pour, harvest, cluck, pet. Они дописаны в конец синтезатора, чтобы
    старые файлы не изменились.
  - Задач стало 14. Тесты: `tests/test_garden.gd`, всего 146 проверок.
  - Шаги сценария: plant:id, time:+минуты; аргументы `--water=5 --pets=1`.
